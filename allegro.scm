#>
#include <allegro5/allegro.h>
<#
(module allegro *
        (import scheme chicken bind foreign foreigners)

        (include "types")
        (include "enums")
        (include "constants")

        (include "base")
        (include "system")
        (include "config")
        (include "path")
        (include "file")
        (include "utf8")
        (include "error")
        (include "altime")
        (include "events")
        (include "mouse")
        (include "timer")
        (include "keyboard")
        (include "joystick")
        (include "display")
        (include "bitmap")
        (include "bitmap_io")
        (include "transformations")
        ;(include "tls")
        ;(include "fshook")
        ;(include "fmaths")
        ;(include "memory")
        ;(include "debug")
        ;(include "threads")
)
